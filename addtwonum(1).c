#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("a =");
    scanf("%d",&a);      //formate specifier needs.for int it is=%d
    printf("b =");       //& is used for keep something on the varibles
    scanf("%d",&b);      //
    sum=a+b;
    printf("so,SUMMATION OF a+b is=%d",sum);

    return 0;

}
